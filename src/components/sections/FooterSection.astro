---
import FooterSocialLink from "@components/ui/links/FooterSocialLink.astro";
import enStrings from "@utils/navigation.ts";
import Icon from "@components/ui/icons/Icon.astro";
import BrandLogo from "@/components/BrandLogo.astro";
import { SITE } from "@data/constants";
import EmailFooterInput from "../ui/forms/input/EmailFooterInput.astro";

// Select the correct translation based on the page's lang prop:
const strings = enStrings

// Define the variables that will be used in this component
const sectionThreeTitle: string = Astro.currentLocale === "fr" ? "Rester à jour" : "Ostanite obavešteni";
const sectionThreeContent: string = Astro.currentLocale === "fr" ? "Restez informé des derniers outils et des offres exclusives." :
  "Ostanite obavešteni o našim popustima i akcijama.";
const crafted: string = Astro.currentLocale === "fr" ? "Fabriqué par" : "Kreiran od strane";
---

<footer class="w-full bg-neutral-300 dark:bg-neutral-900">
  <div
    class="mx-auto w-full max-w-[85rem] px-4 py-10 sm:px-6 lg:px-16 lg:pt-20 2xl:max-w-screen-2xl"
  >
    <div class="flex flex-col sm:flex-row justify-between items-center gap-6">
      <div class="col-span-full lg:col-span-1">
        <!-- Brand Logo -->
        <BrandLogo class="h-auto w-32" />
      </div>

      <div class="col-span-1">
        <h3 class="font-bold text-neutral-800 dark:text-neutral-200 text-center sm:text-left">
          Kontaktirajte nas
        </h3>
        <ul class="mt-3 grid space-y-3 text-center sm:text-left">
          {strings.footerData.contactInfo.map(ci =>(
            <li>
               {ci?.url ? (
                <a
                    href={ci.url}
                    class="inline-flex gap-x-2 rounded-lg text-neutral-600 outline-none ring-zinc-500 transition duration-300 hover:text-neutral-500 focus-visible:ring dark:text-neutral-400 dark:ring-zinc-200 dark:hover:text-neutral-300 dark:focus:outline-none"
                    >
                    <span>{ci.name}:</span><span>{ci.text}</span>
                    </a>
               ) : (
                <span class="text-neutral-600">{ci.name}:</span><span  class="text-neutral-600 dark:text-neutral-400">{ci.text}</span>
               )}
            </li>
          ))}
        </ul>
      </div>

            <!-- An array of links for Product and Company sections -->
            <div class="col-span-1">
              <h3 class=" font-bold text-neutral-800 dark:text-neutral-200 text-center sm:text-left">
                Stranice
              </h3>
              <ul class="mt-3 grid grid-cols-2 lg:grid-cols-1 gap-3">
                {strings.footerData.pageLinks.map(link => (
                  <li >
                    <a
                      href={link.url}
                      class="inline-flex gap-x-2 rounded-lg text-neutral-600 outline-none ring-zinc-500 transition duration-300 hover:text-neutral-500 focus-visible:ring dark:text-neutral-400 dark:ring-zinc-200 dark:hover:text-neutral-300 dark:focus:outline-none"
                    >
                      {link.name}
                    </a>
                  </li>
                ))}
              </ul>
            </div>

      <div class="col-span-2 md:w-1/4">
        <h3 class="font-bold text-neutral-800 dark:text-neutral-200">
          {sectionThreeTitle}
        </h3>
        <form>
          <EmailFooterInput />
          <p class="mt-3 text-sm text-neutral-600 dark:text-neutral-400">
            {sectionThreeContent}
          </p>
        </form>
      </div>
    </div>

    <div
      class="mt-9 grid gap-y-2 sm:mt-12 sm:flex sm:items-center sm:justify-between sm:gap-y-0"
    >
      <div class="flex items-center justify-between">
        <p class="text-sm text-neutral-600 dark:text-neutral-400">
          © <span>2024</span> {SITE.title}. {crafted}
          <a
            class="rounded-lg font-medium underline underline-offset-2 outline-none ring-zinc-500 transition duration-300 hover:text-neutral-700 hover:decoration-dashed focus:outline-none focus-visible:ring dark:ring-zinc-200 dark:hover:text-neutral-300"
            href="callto"
            target="_blank"
            rel="noopener noreferrer">Dragan Bijelic Tel: +381 60 3678388</a
          >.
        </p>
      </div>

      <!-- Social Brands -->
      <div>
        <FooterSocialLink url={strings.socialLinks.facebook}
          ><Icon name="facebookFooter" />
        </FooterSocialLink>

        <FooterSocialLink url={strings.socialLinks.instagram}
          ><Icon name="instagramFooter"/></FooterSocialLink
        >
      </div>
    </div>
  </div>
</footer>
